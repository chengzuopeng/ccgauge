# dependencies
node_modules/
.pnp
.pnp.js
.pnpm-store/
.yarn/install-state.gz

# testing
coverage/

# next.js
.next/
out/
next-env.d.ts

# production
build/
dist/

# misc
.DS_Store
*.pem
.idea/
.vscode/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo

# cache
.cache/
.turbo/

# legacy lockfiles (we use pnpm-lock.yaml)
package-lock.json
yarn.lock

# Marketing site build artefacts (the site itself is tracked under site/)
site/node_modules/
site/dist/
site/.astro/

# playwright-mcp artefacts (console logs + page snapshots dropped here
# by the browser tool during ad-hoc verification runs)
.playwright-mcp/

# Per-developer Claude Code settings — tool permissions each person grants,
# not a property of the project. `.claude/launch.json` IS tracked: it names the
# two dev servers so anyone gets the same ports.
.claude/settings.local.json
